import {style} from 'typestyle';

const styles = {
    message: style({
        display: "flex",
        position: "fixed",
        background: '#4BB543',
        borderRadius: '10px',
        padding: '5px 5px',
        bottom: 50,
        right: 50
    })
}

export default styles;