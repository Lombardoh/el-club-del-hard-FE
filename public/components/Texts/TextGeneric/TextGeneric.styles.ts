import { style } from 'typestyle';

const styles = {
  text: style({
    display: 'flex',
    justifyContent: 'center',
    textAlign: 'center',
    fontWeight: '500',
    lineHeight: '23px',
    color: '#000000',
    margin: '20px auto',
  }),
};

export default styles;
