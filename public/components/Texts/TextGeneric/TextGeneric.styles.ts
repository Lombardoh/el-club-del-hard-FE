import { style } from 'typestyle';

const styles = {
  text: style({
    display: 'flex',
    justifyContent: 'center',
    textAlign: 'center',
    fontWeight: '400',
    lineHeight: '23px',
    color: '#000000',
    margin: 'auto auto',
  }),
};

export default styles;
