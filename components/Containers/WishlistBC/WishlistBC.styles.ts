import {style} from 'typestyle';

const styles = {
    container: style({
        display:'flex',
        flexDirection:'row',
        gap:'20px',
        justifyContent:'flex-end',
        alignItems:'center',
        padding:'10px'
    })
}

export default styles;