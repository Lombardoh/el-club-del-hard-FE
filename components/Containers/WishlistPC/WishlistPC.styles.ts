import {style} from 'typestyle';

const styles = {
    container: style({
        display:'flex',
        flexDirection:'column',
        alignItems:'center',
        gap:'25px',
        padding:'0px 70px 0px 30px',
    })
}

export default styles;