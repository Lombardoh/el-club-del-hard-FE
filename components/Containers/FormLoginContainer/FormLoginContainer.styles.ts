import {style} from 'typestyle';

const styles = {
    container: style({
        display: 'flex',
        flexDirection: 'column',
    }),
    errorMsg: style({
        color:'red',
        marginLeft:'10px'
    })
}

export default styles;